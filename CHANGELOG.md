# CHANGELOG

> 本仓库包含两个配套插件：`material_merger.rb`（按材质合并）+ `su_blender_export.rb`（导出到 Blender）。

## 导出到 Blender 插件 su_blender_export.rb

### v1.0.1（2026-08-15）
- **修复**：导出大模型卡顿——关闭 DAE 导出的预三角化（`triangulated_faces: false`，Blender 导入时自行三角化），消除大模型导出的主要开销
- **新增**：导出前统计面数（`model.statistics[:faces]`），≥50 万面先弹预告（"需要数分钟、进度条是 SketchUp 的、勿强杀"），避免误判卡死

### v1.0.0（2026-08-15）
- **新增**：贴图文件名转英文——遍历所有带纹理材质，用 `texture.write` 导出为 `tex_0001.png`（字母+序号，不重复），存入模型同目录 `<模型名>_textures/`，材质重新指向新文件并恢复纹理尺寸/颜色；已转换的自动跳过（幂等）
- **新增**：一键导出到 Blender——转换贴图 → 导出 Collada（`.dae`，`preserve_instancing` 保留组件实例）→ 自动查找本机 Blender（常见路径 / `BLENDER_EXE` 环境变量）→ 非阻塞启动 Blender 自动导入（清默认场景 / 公制单位 / 聚焦）
- **新增**：控制面板（一键导出按钮 + 仅转换贴图按钮 + 状态区）、菜单、工具栏

## 按材质合并插件 material_merger.rb

### v2.7.1（2026-08-15）
- **变更**：导出到 Blender 功能迁移至独立插件 `su_blender_export.rb`，本插件专注合并 + 清理

### v2.7.0（2026-08-15）
- **新增**：一键导出到 Blender（面板按钮 + 菜单）——导出保留实例的 DAE 并自动启动 Blender 导入（已随 v2.7.1 迁移至独立插件）

### v2.6.3（2026-08-15）
- **改进**：编辑态防护提示改为「是否自动退出编辑并继续」——选"是"自动调用 `model.close_active` 退出，免去手动按 Esc

### v2.6.2（2026-08-15）
- **修复**：清理未使用数据后卡死——移除清理后的 `active_view.refresh`（大模型下全量重绘极慢）；清理 0 个时用 `abort_operation` 代替 `commit_operation`（无改动不产生撤销/内部重算）
- **修复**：`purge_unused_definitions` 改为"先收集再删除"（消除迭代集合时修改集合的隐患）
- **改进**：异步分块间隙不再每块刷新视图（大模型下每 0.15s 全量重绘是卡顿隐患），仅完成时刷新一次

### v2.6.1（2026-08-15）
- **改进**：控制面板重开入口可见性——菜单项改名「打开控制面板...」、工具栏按钮改名「打开面板」、面板底部加"关闭后重开"提示行

### v2.6.0（2026-08-15）
- **新增**：控制面板「清理未使用数据」按钮 + 菜单入口（独立于合并流程，带确认与回滚）

### v2.5.0（2026-08-15）
- **修复**：点"开始合并"即卡死——分块粒度从"材质桶"下沉到"单个实体"（时间片内逐实体处理），同材质桶内几万~几十万实体不再单块长时间占满主线程
- **重构**：抽取 `copy_entity_to_group`（单实体复制，异步/同步路径共用）

### v2.4.0（2026-08-15）
- **新增**：常驻控制面板（「开始合并」按钮 + 取消按钮 + 进度条 + 状态区）；面板打开时即作为进度载体（替代临时进度弹窗）
- **重构**：进度显示接口统一（ControlPanel / ProgressDisplay 实现同一接口），`js_escape` 提为模块级

### v2.3.0（2026-08-15）
- **新增**：异步分块执行——大任务按时间片（0.15s）分块，`UI.start_timer` 链式调度让出事件泵，界面不冻结；防重入标志

### v2.2.0（2026-08-15）
- **新增**：HtmlDialog 进度弹窗（百分比 + 当前材质组名 + 取消按钮）；小任务状态栏进度；SU 2017/2018 自动降级

### v2.1.0（2026-08-15）
- **修复**（场景走查 P-01/P-03）：编辑态（`active_path` 非空）防护；`puts` 调试输出改 `DEBUG_MODE` 开关

### v2.0.0（2026-08-15）
- **修复**：v1 致命 bug——`transform_by_entities` 不是 SketchUp API；改用社区标准"实例化定义 + 炸开"内核批量复制（依据官方论坛 Copy entities 帖 / api-issue-tracker #41）
- **新增**：组件实例保留为实例（共享定义，文件最紧凑）；批量删除 `erase_entities`；复制失败实体不删；出错整体回滚

### v1.0.0（2026-08-15）
- **新增**：按材质合并（组→炸开复制、组件实例→保留）；可选清理未使用组件定义；选中范围/全模型两种模式；单步撤销
